import {StyleSheet} from 'react-native';

export const buttonStyle = StyleSheet.create({
  disabled: {},
  pressed: {},
  default: {
    width: 50,
    height: 50,
    backgroundColor: '#000',
    borderRadius: 999,
    justifyContent: 'center',
    alignItems: 'center',
    margin: 100,
  },
});
